# ---- BOT Chain network ----
BOT_RPC_URL=https://rpc.botchain.ai/
BOT_CHAIN_ID=677
BOT_EXPLORER_URL=https://scan.botchain.ai/

# ---- Deployment / agent wallets (NEVER commit real values) ----
# The deployer owns the contracts and the AttesterRegistry.
DEPLOYER_PRIVATE_KEY=
# The single authorized attester in v1 (separate wallet recommended).
ATTESTER_PRIVATE_KEY=

# ---- AI context layer ----
# Server-side only. Never exposed to the frontend. If empty, the AI layer
# runs in a deterministic "INSUFFICIENT_DATA / no-LLM" fallback so the system
# still works end to end.
ANTHROPIC_API_KEY=

# ---- Frontend (VITE_ vars are inlined at build time) ----
VITE_BOT_RPC_URL=https://rpc.botchain.ai/
VITE_CHAIN_ID=677
VITE_EXPLORER_URL=https://scan.botchain.ai/
VITE_ASSET_ADDRESS=
VITE_GATE_ADDRESS=
VITE_ATTESTER_ADDRESS=

# ---- Optional: point the agent at a non-mainnet RPC for dev (defaults to localhost:8545) ----
SLUICE_RPC_URL=http://127.0.0.1:8545
# seconds an attestation is valid after signing (agent config, default 600)
ATTESTATION_TTL=600
